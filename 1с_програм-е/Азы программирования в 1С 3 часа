Запросы в 1С за 3 часа 
Разбирает что такое переменные
Код пишем в модуле объекта конфигурации 

А = 1; 
Б = 2;
В = А * Б;

Показывает как работать с коментариями
Показывает как работать с условиями

ЧислоА = 15;
ЧислоБ = 10;

Если ЧислоА > ЧислоБ Тогда 
	РазницаПоМодулю = ЧислоА - ЧислоБ;
Иначе 
	РазницаПоМодулю = ЧислоБ - ЧислоА;
КонецЕсли;

Теперь смотрим краткую форму условия

обычно краткое условие используется для простых случаев, иначе страдает читаемость кода 

КраткаяФормаРазницаПоМодулю = ?(ЧислоА > ЧислоБ, ЧислоА - ЧислоБ, ЧислоБ - ЧислоА);



Пробуем условие ИначеЕсли

Цвет = "красный";

Если Цвет = "красный" Тогда
	КодЦвета 255;
ИначеЕсли Цвет = "синий" Тогда
	КодЦвета = 199;
ИначеЕсли Цвет = "белый" Тогда
	КодЦвета = 0;
Иначе
	КодЦвета = -1;
КонецЕсли;

Логические операции 

ЧислоА = 7;
ЧислоБ = 5;
ЧислоА = ЧислоБ = 5;
Сообщить(ЧислоА); // Да - такой будет ответ 
оператор присваивания работает справа налево 
второе равно операция сравнения, первое это присваивание 

ЧислоА = 7;
ЧислоБ = 6;
ЧислоА = ЧислоБ = 5;
Сообщить(ЧислоА); // НЕТ - такой будет ответ 

Логические операции и сложные условия


// кгшичества продаж > 10 И сумма продаж > 500 руб. - "важный клиент"
// в противном случае - "неважные клиент"

КоличествоПродаж = 15;
СуммаПродаж = 500;

Если КоличествоПродаж  > 10 И СуммаПродаж > 500
	Сообщить("Важный клиент"); 
Иначе
	Сообщить("не важные клиент");
КонецЕсли 

Закончил на 1 30 

Цикличный оператор 

Например страндартная задача - ищем сумму чисел 

Процедура КнопкаВыполнитьНажатие(Кнопка)
	НачальноеЧисло = 1;
КонечноеЧисло = 100;
СуммаЧисел = 0;
Пока НачальноеЧисло <= КонечноеЧисло Цикл     
	СуммаЧисел = СуммаЧисел + НачальноеЧисло;
	НачальноеЧисло = НачальноеЧисло + 1;
КонецЦикла;
Сообщить(СуммаЧисел);
КонецПроцедуры

я решил ее сам 

Изучаем цикл по счетчику
Делаем ту же задачу но с помощью цикла по счетчику

Для ТекущееЧисло = 1 По 100 Цикл
	СуммаЧисел = СуммаЧисел + ТекущееЧисло;
КонецЦикла;
Сообщить(СуммаЧисел);

Цикл по коллекции(Массиву) используется Для Каждого
Мы хотим просмотреть все элементы коллекции
// Заполнение массива
МассивЧисел = Новый Массив;
Для ТекЧисло = 1 По 100 Цикл 
	МассивЧисел.Добавить(ТекЧисло);
КонецЦикла;

// сложение чисел из массива

СуммаЧисел = 0

Для каждого ТекЧисло Из МассивЧисел Цикл
	СуммаЧисел = СуммаЧисел + ТекЧисло;
КонецЦикла;


Сообщить(СуммаЧисел);

Попробую решить задачу Сумма четных чисел 


Процедура ОсновныеДействияФормыОсновныеДействияФормыВыполнить(Кнопка)
	МассивЧисел = Новый Массив;
	Для ТекЧисло = 1 По 100 Цикл 
		МассивЧисел.Добавить(ТекЧисло);
	КонецЦикла;
	
	// сложение чисел из массива
	
	СуммаЧисел = 0;
	
	Для каждого ТекЧисло Из МассивЧисел Цикл 
		Если ТекЧисло % 2 = 0 Тогда
			СуммаЧисел = СуммаЧисел + ТекЧисло;
		Иначе 
			Продолжить;
		КонецЕсли;
	КонецЦикла; 
Сообщить(СуммаЧисел);
КонецПроцедуры

Еще задача, прервать цикл, как только СуммаЧисел будет больше 1000

Процедура ОсновныеДействияФормыОсновныеДействияФормыВыполнить(Кнопка)
	МассивЧисел = Новый Массив;
	Для ТекЧисло = 1 По 100 Цикл 
		МассивЧисел.Добавить(ТекЧисло);
	КонецЦикла;
	
	// сложение чисел из массива
	
	СуммаЧисел = 0;
	
	Для каждого ТекЧисло Из МассивЧисел Цикл 
		Если ТекЧисло % 2 = 0 Тогда
			СуммаЧисел = СуммаЧисел + ТекЧисло;
		Иначе 
			Продолжить;
		КонецЕсли;  

		Если СуммаЧисел >= 1000 Тогда
			Прервать; // выходит из цикла
		Конецесли;

	КонецЦикла; 
Сообщить(СуммаЧисел);
КонецПроцедуры


Примитивные типы данных 

Добавляет Справояник Товары, Реквизит 
Объясняет про числа, строки и дату, как переменные работают и складываются 

ТекДата = '20200819202931'; // тяжело привыкнуть к такой записи даты
Сообщить(ТекДата); 

Объясняет что к дате надо прибавлять секунды
ЗавтрашнийДень = ТекДата + 60*60*24; // или можно записать вот так, что выгладит более понятно

Работаем с типом переменной 

ПеременнаяНеизвестногоТипа = "тест";

Если ТипЗннч(ПеременнаяНеизвестногоТипа) = Тип("Строка") Тогда
	Сообщить("Это строка");
ИначеЕсли ТипЗннч(ПеременнаяНеизвестногоТипа) = Тип("Число") Тогда // этот код работает 
	Сообщить("Это Число");
Конецесли;

Теперь он блоки кода делить на проведуры и снизу он их вызывает  2:37:00
Процедура не выполнится пока ее не вызовешь или если она не привязана к кнопке 

Работаеи с Функцией 

Функция СуммаЧисел()
	Число1 = 19;
	Число2 = 20;
	СуммаЧисел = Число1 + Число2;
	Возврат СуммаЧисел; 
КонецФункции

Результат = СуммаЧисел(); // В отличие от процедуры, Функцию можно присвоить к переменной 
Сообщить(Результат);

// если мы хотим что бы функция считала любые числа, то надо что бы числе были извне

Функция СуммаЧисел(ИмяПараметра1, ИмяПараметра2, ИмяПараметра3 = 11 ) // ИмяПараметра3 это не обязательный и доп параметр, который не обязательно передавать
	
	СуммаЧисел = ИмяПараметра1 + ИмяПараметра2 +  ИмяПараметра3; 
	Возврат СуммаЧисел; 
КонецФункции


Число1 = 19;
Число2 = 20;
Результат = СуммаЧисел(Число1, Число2); // В отличие от процедуры, Функцию можно присвоить к переменной 
Результат2 = СуммаЧисел(55, 22, 66); // меняем параметр3, который был по умолчанию изначально 11 
Сообщить(Результат);
Сообщить(Результат2);

Структура програмного модуля такогова
1. Сначала обьявление переменых
2. Потом процедуры и КонецФункции
3. Описываются операторы и используем переменный, процедуры и функции, которые описаны были выше 

Объясняет справку 3:00:00
 
 Рассматриваем Встроенные функции для чисел
 Чаще всего использудются функции Цел() и Окр(), остальные очень редко использудются

 Процедура Встроенныефункции_Числа()
 	ДробноеЧисло = 4.25;
 	Результат = Цел(ДробноеЧисло);
 	Сообщить(Результат) // 4
 	Результат2 = Окр(ДробноеЧисло, 1);
 	Сообщить(Результат2) // 4.3
 	// у окр есть третьий парамент, РежимОкругления.Окр15как10 и РежимОкругления.Окр15как20 тут решается, пятерку округляем в большую сторону или в меньшую
 КонецПроцедуры

  Процедура Встроенныефункции_Числа()

В веб клиенте не рекомендуется использовать модальные окна 

Базовые функции со строкаи и датами

Процедура Встроенныефункции_Строки();
	ТестоваяСтрока = "    красный, синий, желтый, зеленый   ";

	ДлинаСтроки = СтрДлина(ТестоваяСтрока); // узнать длину строки
	ОбрезанаяСтрока = СокрЛП(ТестоваяСтрока); // обрезаем лишние пробелы
	СловоКрасный = Лев(ТестоваяСтрока, 11); // нам нужно получить 12 символов
	СловоСиний = Сред(ТестоваяСтрока, 14, 5); 
	ПозицияЗапятой = СтрНайти(ТестоваяСтрока, ",");
    Сообщить(СловоКрасный);
	Сообщить(СловоСиний);
	Сообщить(ПозицияЗапятой);
	// показывает ВРег,НРег,ТРег

	// Заменить запятые на точки
	СтрокаСДругимРазделителем = СтрЗаменить(ТекстоваяСтрока, ",", ".");

	// Делаем из строки массив

	МассивСлов = СтрРазделить(ТестоваяСтрока, ",");

	КонецПроцедуры

Встроенныефункции_Строки();