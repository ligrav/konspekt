11 Книга событий из истории

Задачу долго писать, просто оставлю ссылку и решение что бы я мог потом посмотреть 

https://helpme1s.ru/uprazhneniya-po-vnutrennemu-yazyku-programmirovaniya-1s-8-3-chast-12-kniga-sobytij-iz-istorii


Конфигурация - Модуль

События = Новый СписокЗначений();
События.Добавить(1961,"Первый полет в космос"); 
События.Добавить(988,"Крещение Руси");  
Пока Истина Цикл
СписокКоманд = Символы.ПС  + 
" ================================== " 
"[1] - Ввести новое событие "
"[2] - Очистить список событий "
"[3] - сортировать события по году "
"[4] - вывести все события на определенную дату "
" ================================== " ;

Предупреждение(СписокКоманд,,"Внимание!"); 

Результат = 0; 
ВвестиЧисло(Результат);            
Если Результат = 1 Тогда
	//  ввести новое событие
	КнигаСобытий.ВвестиСобытий(События);
	КнигаСобытий.ВвестиРезультат(События);
ИначеЕсли Результат = 2 Тогда
	КнигаСобытий.ОчиститьСобытия(События);  
	
ИначеЕсли Результат = 3 Тогда
	События.СортироватьПоЗначению(НаправлениеСортировки.Возр);
	КнигаСобытий.ВвестиРезультат(События);
ИначеЕсли Результат = 4 Тогда
	КнигаСобытий.ВыборПоГоду(События);

Иначе
	ОткрытьЗначение( "Вы выходите из программы");
	Прервать;
КонецЕсли;

КонецЦикла;

					Общий модуль - КнигаСобытий

Процедура ВвестиРезультат(СписокСобытий)   Экспорт 
	Для Каждого Элемент Из СписокСобытий  Цикл
		ОткрытьЗначение(
		Элемент.Представление + " в " +
		Строка(Элемент.Значение)
		);
	КонецЦикла;
	
КонецПроцедуры

Процедура ВвестиСобытий(СписокСобытий)   Экспорт     
	Строка = ""; 
	Число = 0;  
	ОткрытьЗначение("Введите событие");
	ВвестиСтроку(Строка);  
	ОткрытьЗначение("Введите год");
	ВвестиЧисло(Число);
	СписокСобытий.Добавить(Число, Строка);
КонецПроцедуры        

Процедура ОчиститьСобытия(СписокСобытий) Экспорт
	
	Результат = Вопрос("Вы уверены в своих действиях?", РежимДиалогаВопрос.ДаНет);
	Если Результат = КодВозвратаДиалога.Да Тогда	
		СписокСобытий.Очистить();	
		ОткрытьЗначение("Список событий чист");
	Иначе 
		ОткрытьЗначение("ну и правильно");
	КонецЕсли;
КонецПроцедуры        

Процедура ВыборПоГоду(СписокСобытий) Экспорт
	
	ОткрытьЗначение("Выберите год");
	Число = 0;
	ВвестиЧисло(Число);
	
	Результат =  СписокСобытий.НайтиПоЗначению(Число); 
	Если Результат = Неопределено Тогда   
		ОткрытьЗначение("В этот год ничего не произошло"); 
	Иначе
		
		ОткрытьЗначение(СписокСобытий.НайтиПоЗначению(Число));
		
	КонецЕсли;
	
КонецПроцедуры

